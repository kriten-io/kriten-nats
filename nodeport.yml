apiVersion: v1
kind: Service
metadata:
  name: nats-nodeport
spec:
  selector:
    app.kubernetes.io/component: nats
    app.kubernetes.io/instance: nats
    app.kubernetes.io/name: nats
  type: NodePort
  ports:
    - protocol: TCP
      port: 4222
      targetPort: 4222
      nodePort: 30222
