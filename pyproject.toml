[project]
name = "kriten-nats"
version = "0.1"
description = "Start Kriten jobs with NATS"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.13.5",
    "asyncio>=4.0.0",
    "nats-py>=2.14.0",
]
